apiVersion: v1
kind: Service
metadata:
  name: codegen
  labels:
    app: codegen
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: codegen
